#include <iostream>
using namespace std;
int main() {
    int n, max, min, x;
    cin>>n;
    cin>>x;
    max=x;
    min=x;
    for (int i=1;i<n;i++){
        cin>>x;
        if(max<x) max=x;
        if(min>x) min=x;
    }
    cout<<max<<" "<<min;
}
